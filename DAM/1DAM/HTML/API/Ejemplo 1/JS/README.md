# 🚀 JavaScript Basics Practice | Práctica de Fundamentos de JavaScript

## 📋 Description | Descripción
**EN:** This project is a comprehensive collection of introductory JavaScript exercises designed to demonstrate core programming concepts. It covers DOM manipulation, different variable scopes, event handling, and basic arithmetic logic, serving as a solid foundation for interactive web development.

**ES:** Este proyecto es una colección completa de ejercicios introductorios de JavaScript diseñados para demostrar conceptos fundamentales de programación. Abarca la manipulación del DOM, diferentes alcances de variables, manejo de eventos y lógica aritmética básica, sirviendo como una base sólida para el desarrollo web interactivo.

## ✨ Key Features | Características Clave
- **DOM Interaction:** Dynamically updating text content using `innerHTML` and `innerText`. | **Interacción con el DOM:** Actualización dinámica de contenido mediante `innerHTML` e `innerText`.
- **Variable Management:** Practical implementation of `var`, `let`, and `const` to understand scoping. | **Gestión de Variables:** Implementación práctica de `var`, `let` y `const` para entender el alcance (scope).
- **Functional Logic:** Use of functions to process data, perform calculations, and return values. | **Lógica Funcional:** Uso de funciones para procesar datos, realizar cálculos y devolver valores.
- **User Input Handling:** Capturing and validating data from HTML input elements. | **Manejo de Entradas:** Captura y validación de datos provenientes de elementos de entrada HTML.
- **Feedback Mechanisms:** Implementation of `console.log()` and `window.alert()` for debugging and user alerts. | **Mecanismos de Feedback:** Implementación de `console.log()` y `window.alert()` para depuración y alertas al usuario.

## 🛠️ Tech Stack | Tecnologías
- **HTML5:** Structure and UI elements. | Estructura y elementos de la interfaz.
- **JavaScript (ES6+):** Logic, interactivity, and data processing. | Lógica, interactividad y procesamiento de datos.

## 📂 Project Structure | Estructura del Proyecto
```text
JS/
├── index.html   # Main interface and element structure | Interfaz principal y estructura.
└── script.js    # Logic, functions, and DOM manipulation | Lógica, funciones y manipulación del DOM.
```

## 🚀 How to Run | Cómo Ejecutar
**EN:**
1. Clone or download this folder to your local machine.
2. Locate the `index.html` file.
3. Open `index.html` in any modern web browser (Chrome, Firefox, Edge, etc.).
4. To see the logic in action, you can open the developer tools (F12) and check the "Console" tab.

**ES:**
1. Clona o descarga esta carpeta en tu máquina local.
2. Localiza el archivo `index.html`.
3. Abre `index.html` en cualquier navegador web moderno (Chrome, Firefox, Edge, etc.).
4. Para ver la lógica en acción, puedes abrir las herramientas de desarrollador (F12) y revisar la pestaña "Consola".

<!-- AI-GENERATED-README -->