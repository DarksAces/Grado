# Definimos una función que suma dos números
def suma(a, b):
    resultado = a + b
    return resultado # Devolvemos el resultado de la suma
# Llamamos a la función pasando dos argumentos
print(suma(5, 3)) # Imprime: 8