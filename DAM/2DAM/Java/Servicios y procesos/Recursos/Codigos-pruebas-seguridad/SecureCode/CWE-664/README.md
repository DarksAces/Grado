# CWE-664

Contiene ejemplos relacionados con problemas de formato y manejo de entrada que pueden conducir a vulnerabilidades (varios sub-CWEs incluidos). Busca `compliant*.py` y `noncompliant*.py` en cada subcarpeta para ver ejemplos.
