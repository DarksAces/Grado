# CWE-117

Ejemplos y notas sobre `CWE-117` (inyección en logs y salida no segura). Contiene `compliant` y `noncompliant`.

Recomendación: sanea datos antes de escribir en logs y evita leak de información sensible.
