# CWE-89

Ejemplos relativos a `CWE-89` (inyección SQL). Contiene ejemplos `compliant` y `noncompliant` para practicar consultas seguras y parametrizadas.

Consejo: usa consultas parametrizadas y ORM cuando sea posible.
