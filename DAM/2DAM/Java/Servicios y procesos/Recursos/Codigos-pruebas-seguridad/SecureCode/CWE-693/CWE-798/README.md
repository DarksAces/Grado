# CWE-798

Ejemplos y scripts sobre `CWE-798` (credenciales codificadas). Incluye `compliant` y `noncompliant`, así como scripts de configuración (`start.sh`, `setup.sh`).

Recomendación: no mantengas credenciales en el código; usa variables de entorno o gestores de secretos.
